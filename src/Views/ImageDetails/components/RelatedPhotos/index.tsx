import React from 'react'
import { SectionTitle } from '../../styles'

function index() {
    return (
        <div>
            <SectionTitle>Related Photo</SectionTitle>
        </div>
    )
}

export default index
